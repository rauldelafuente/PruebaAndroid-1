<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.isec.ans.mysketches">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".SketchesApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SketchesMainActivity" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChooseColorActivity"  android:screenOrientation="portrait"/>
        <activity android:name=".SketchActivity"  android:screenOrientation="portrait"/>
        <activity android:name=".ChooseFromGalleryActivity"  android:screenOrientation="portrait"/>
        <activity android:name=".TakePhotoActivity"  android:screenOrientation="portrait"/>
        <activity android:name=".ListActivity" android:screenOrientation="portrait"/>
    </application>

</manifest>